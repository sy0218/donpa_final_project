apiVersion: v1
kind: PersistentVolumeClaim
metadata:
        name: sang-csv-pvc
spec:
        accessModes:
                - ReadWriteMany
        resources:
                requests:
                        storage: 10Gi

