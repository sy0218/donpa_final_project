apiVersion: v1
kind: PersistentVolumeClaim
metadata:
        namespace: sy
        name: model-pvc
spec:
        accessModes:
                - ReadWriteMany
        resources:
                requests:
                        storage: 5Gi
